<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="text_base" parent="@android:style/TextAppearance.Small"/>

    <style name="field_common" parent="text_base">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:singleLine">true</item>
        <item name="android:layout_marginTop">4dp</item>
        <item name="android:layout_marginBottom">4dp</item>
          <!-- avoid setting left/right margins, because these cause skewed
            successive horizontal alignments with RelativeLayout! (at least in Android 2.2) -->
        <item name="android:textColor">@android:color/primary_text_light</item>
    </style>

    <style name="field" parent="field_common">
        <item name="android:layout_width">70dp</item>
    </style>

    <style name="clr_button" parent="text_base">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:textColor">@android:color/primary_text_light</item>
        <item name="android:background">@android:drawable/ic_input_delete</item>
    </style>

    <color name="unknown_value">#fffcc9</color>
    <color name="valid_value">#ff7ac074</color>
    <color name="error_value">#ff817f</color>
</resources>
